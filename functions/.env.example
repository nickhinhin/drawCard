# Fail-closed comma-separated allowlists. Keep UID as the permanent identity key.
ADMIN_UID_ALLOWLIST=firebase_uid_1,firebase_uid_2
# Optional second check. Email is never accepted without both UID allowlisting and admin claim.
ADMIN_EMAIL_ALLOWLIST=owner@example.com,manager@example.com
